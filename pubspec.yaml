name: onscreen_logger
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.2.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: any

  get: ^4.6.5
  word_generator: ^0.4.4
  dio: ^5.5.0
  fluttertoast: ^8.2.5
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  assets:
    - assets/ic_copy.svg
    - assets/log.svg

